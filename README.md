# Stevo XOX clone

Static XOX/Connect Four experience. Run it locally with `npm start` and run the dependency-free regression checks with `npm test`.

The project currently ships compiled browser assets. See [ASSET_PROVENANCE.md](ASSET_PROVENANCE.md) before publishing or redistributing it.
